# Чеклист перед публикацией

## Проверка кода и сборки

- [ ] `npm install`
- [ ] `npm run test:run` — все тесты зелёные (54 теста)
- [ ] `npm run build` — сборка без ошибок
- [ ] В коде нет отладочных `console.log` (допустимы `console.warn`)

## Кто такой админ и как войти

- **Без Firebase** (по умолчанию): админ — любой, кто откроет `/welcome_me`. Флаг сохраняется в localStorage. Выход — «Выйти из режима админа» или `/?admin=0`.
- **С Firebase**: в `.env` заданы `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_APP_ID` и `VITE_ADMIN_EMAIL` (ваш email или несколько через запятую). Тогда по «Войти в режим админа» открывается форма входа; админом считается только пользователь с указанным email.

## Секреты и окружение

- [ ] Скопирован `.env.example` в `.env`
- [ ] В `.env` заполнены нужные переменные:
  - `VITE_MISTRAL_API_KEY` — для перевода в словаре (в dev ключ идёт через прокси)
  - Опционально: `VITE_FIREBASE_*` и `VITE_ADMIN_EMAIL` — для входа админа по паролю
- [ ] Файл `.env` не добавлен в git (`git status` не должен показывать .env)

## Хостинг

- [ ] На хостинге в настройках билда заданы переменные окружения для production (Mistral, Firebase), если нужны
- [ ] Сайт на **urbanplanner.page** — в `vite.config.ts` задано `base: '/'` (уже настроено)
- [ ] SPA: сервер отдаёт `index.html` на все маршруты

## Проверка функций после деплоя (вручную)

### Главная и админка
- [ ] Открыть `/` — видна главная, кнопки (Редактор, Калькуляторы, Блог и т.д.)
- [ ] Открыть `/welcome_me` — форма Firebase или сразу переход в режим админа (появится «Выйти из режима админа», блок «Синхронизация с GitHub»)
- [ ] В режиме админа на главной видны ссылки: Словарь, Заметки, RSS подписки (админ)
- [ ] «Выйти из режима админа» или `/?admin=0` — выход, админ-ссылки исчезают
- [ ] Гостевой доступ: в `.env` задан `VITE_GUEST_EMAIL`; пользователь с этим email видит только Планировщик и Метеостанцию

### Редактор и калькуляторы
- [ ] `/editor` — редактор открывается, можно добавить блоки, сохранить черновик, нажать «Опубликовать» (ввод названия и slug)
- [ ] После публикации калькулятор появляется в списке на `/calculators` (если есть данные из репо или localStorage)
- [ ] `/calculators` — список калькуляторов; клик по калькулятору — открывается расчёт

### Блог
- [ ] `/blog` — список постов (из репо или пусто)
- [ ] В режиме админа: кнопки «+ Новый пост», «Выгрузить последний сэйв из репо», «Загрузить в репо», «Экспорт для GitHub»; можно создать/редактировать пост, сохранить
- [ ] Ссылка «Подписаться на блог (RSS)» и «Копировать ссылку» — для выдачи подписчикам (`/feed.xml`)
- [ ] Открытие поста по ссылке `/blog/:slug` — пост отображается

### Заметки (только админ)
- [ ] Без админа: `/admin/notes` — сообщение «Доступ только для админа»
- [ ] В режиме админа: `/admin/notes` — заметки, можно создать, редактировать, экспорт для GitHub

### Словарь (только админ)
- [ ] Без админа: `/dictionary` — «Доступ только для админа»
- [ ] В режиме админа: словарь открывается, перевод (если задан Mistral — «Проверить доступ к Mistral»)

### Порядок окон (главная)
- [ ] В режиме админа на главной — кнопка ✏️ (редактировать страницу); кнопки «Выгрузить последний сэйв из репо» и «Загрузить в репо»; изменить порядок секций, «Сохранить» — порядок сохраняется

### RSS подписки (только админ)
- [ ] `/rss` — только для админа; блок «Подписаться на этот блог (RSS)» со ссылкой на `/feed.xml`; списки подписок, кнопки «Выгрузить последний сэйв из репо» и «Загрузить в репо» (если синхронизация настроена)

### Метеостанция (админ или гость)
- [ ] `/weather` — доступна после входа (админ или гостевой email); данные из Google Таблицы или вставка CSV вручную

### Синхронизация с GitHub (опционально)
- [ ] В режиме админа на главной открыть «Синхронизация с GitHub», ввести owner, repo, ветку, PAT, «Сохранить», «Проверить подключение» — успех
- [ ] В каждом разделе (блог, заметки, словарь, планировщик, порядок окон, редактор калькуляторов, RSS): кнопки «Выгрузить последний сэйв из репо» и «Загрузить в репо»
- [ ] После изменений данные автоматически пушатся в репо с дебаунсом (если синхронизация настроена)
